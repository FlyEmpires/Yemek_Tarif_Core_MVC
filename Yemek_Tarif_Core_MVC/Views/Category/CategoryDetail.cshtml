@model List<EntityLayer.Concrete.Recipe>
@{
    ViewData["Title"] = "CategoryDetail";
    Layout = "~/Views/Shared/UserLayout.cshtml";
}

<section class="main-content-w3layouts-agileits">
    <div class="container">
        <h3 class="tittle">@(Model.Count > 0 ? Model.FirstOrDefault().Category.CategoryName : "Bu kategoriye henüz yemek eklenmemiş")</h3>
        <div class="inner-sec">
            <!--left-->
            <div class="left-blog-info-w3layouts-agileits text-left">
                @*<table class="table table-bordered">
                <tr>
                <th>Yemek Adı</th>
                <th>Yemek Açıklama</th>
                <th>Yemek Malzeme</th>

                </tr>
                <tbody>
                @foreach (var item in Model)
                {
                <tr>
                <td>@item.ReceipeName</td>
                <td>@item.ReceipeDescription</td>
                <td>@item.ReceipeIngredient</td>
                </tr>
                }

                </tbody>

                </table>*@
                <div class="row">
                    @foreach (var item in Model)
                    {

                        <div class="col-lg-4 card" @*style="background-color:beige;"*@>
                            <a href="/Recipe/RecipeReadAll/@item.ReceipeID">
                                <img src="/@item.ReceipeImage" class="card-img-top img-fluid" alt="">
                            </a>
                            <div class="card-body">
                                <ul class="blog-icons my-4">
                                    <li>
                                        <a href="#">
                                            <i class="far fa-calendar-alt"></i> @item.CreateDate.ToString("g")
                                        </a>
                                    </li>
                                    <li class="mx-2">
                                        <a href="#">
                                            <i class="far fa-comment">21 </i>
                                        </a>
                                    </li>
                                    <li>
                                        <a href="#">
                                            <i class="fas fa-eye" style="margin-top:20px;"> @item.Category.CategoryName</i> 2000 @*Eager Loading -- include yöntemi ile ilişkili alana ait verileri getirdik*@
                                        </a>
                                    </li>

                                </ul>
                                <h5 class="card-title">
                                    <a href="single.html">@item.ReceipeName</a>
                                </h5>
                                <p class="card-text mb-3">
                                    @*@if (item.ReceipeDescription.Length > 50)
                                {
                                <i class="far fa-comment">@item.ReceipeDescription.Substring(0,item.ReceipeDescription.Substring(0,130).LastIndexOf(" ")) </i>
                                }
                                else
                                {
                                <i class="far fa-comment">@item.ReceipeDescription</i>

                                }*@
                                    @(item.ReceipeDescription.Length > 50 ? item.ReceipeDescription.Substring(0, item.ReceipeDescription.Substring(0, 130).LastIndexOf(" ")) : item.ReceipeDescription)
                                </p>
                                <a href="/Recipe/RecipeReadAll/@item.ReceipeID" class="btn btn-primary read-m" style="border-radius:30px;">Devamını Oku</a>
                            </div>
                        </div>
                    }
                    @*Pagination*@

                    @*					<nav aria-label="Page navigation example" >
                    <ul class="pagination justify-content-left mt-4">
                    <li class="page-item disabled">
                    <a class="page-link" href="#" tabindex="-1">Önceki</a>
                    </li>
                    <li class="page-item">
                    <a class="page-link" href="#">1</a>
                    </li>
                    <li class="page-item">
                    <a class="page-link" href="#">2</a>
                    </li>
                    <li class="page-item">
                    <a class="page-link" href="#">3</a>
                    </li>
                    <li class="page-item">
                    <a class="page-link" href="#">Sonraki</a>
                    </li>
                    </ul>
                    </nav>
                    *@
                </div>
                <!--//left-->
            </div>
        </div>
    </div>
</section>
